extends ../component/layout
block title
    title Vue 
    link(rel="stylesheet" type="text/css" href="./public/puppies/style.css")
    script(src="https://cdn.jsdelivr.net/npm/vue@2/dist/vue.js")
    script(src="/script/puppies.js")

block content
  h1 Vue

  div(id="app")
    div(class="cart")
      p({{ 'Cart' + cart.length }})
    //- product(:premium="premium" @add-to-cart="updateCart" @remove-from-cart="removeCart"product)
