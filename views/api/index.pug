extends ../component/layout
block title
    title api 
      
block content
  h1 api orderCreate
    .col
      form(action="/api" method='post')
        label(for='name') Name + Preis:
        //- #inputFields
        input(name='name' type='text' placeholder=lastName)
        label(for='price')
        //- #inputFields
        input(name='price' type='number' placeholder=lastPrice)
        input.btn(type='submit' value='Submit') 
      form(action='/api' method='get')
        input.btn(type='submit' value='get')
        p Antwort:
        table(style='width:100%', border='1')
          tr
            th Id
            th Name
            th Price
            th Empfänger
          tr
            th(href=`http://localhost:3000/api/`+`#{lastId}`) #{lastId} 
            td #{lastName}
            td #{lastPrice}
            td #{lastRecipient}
          tr
            th #{lastId}
            td #{lastName}
            td #{lastPrice}
            td #{lastRecipient}
          tr
            th #{lastId}
            td #{lastName}
            td #{lastPrice}
            td #{lastRecipient}
          tr
            th #{lastId}
            td #{lastName}
            td #{lastPrice}
            td #{lastRecipient}
          tr
            th #{lastId}
            td #{lastName}
            td #{lastPrice}
            td #{lastRecipient}
          tr
            th #{lastId}
            td #{lastName}
            td #{lastPrice}
            td #{lastRecipient}
          tr
            th #{lastId}
            td #{lastName}
            td #{lastPrice}
            td #{lastRecipient}
          tr
            th #{lastId}
            td #{lastName}
            td #{lastPrice}
            td #{lastRecipient}
          tr
            th #{lastId}
            td #{lastName}
            td #{lastPrice}
            td #{lastRecipient}
          tr
            th #{lastId}
            td #{lastName}
            td #{lastPrice}
            td #{lastRecipient} 