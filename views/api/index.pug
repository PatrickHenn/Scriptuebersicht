extends ../component/layout
block title
    title api 
      
block content
  h1 api orderCreate
    .col
      form(action="/api" method='post')
        p Name + Preis:
        label(for='name')
          input(name='name' type='text' placeholder='Name')
        label(for='price')
          input(name='price' type='number' placeholder='Price')
          input.btn(type='submit' value='Submit') 
      form(action='/api' method='get')
         input.btn(type='submit' value='get')
      p Antwort:
          pre=JSON.stringify(result)
      table(style='width:100%', border='1')
          tr
            th Id
            th Name
            th Price
            th Empfänger
            - for (let i = 0; i < 10; ++i) {
          tr
            td=getOrder[i].lastId
            td=getOrder[i].lastName
            td=getOrder[i].lastPrice
            td=getOrder[i].lastRecipient
            -}